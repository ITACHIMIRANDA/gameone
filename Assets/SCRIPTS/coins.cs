using UnityEngine;
using System.Collections;

public class coins : MonoBehaviour {
    /*Para el audio
    public AudioClip coinsound2 ;
    public int CoinValue;
    public GameObject pickupparticleefect;

    private AudioSource audio2;
    private bool _triggered;


    //Antes que void star
   void Awake ()
    {
        audio2 = gameObject.GetComponent<AudioSource>();
    }
    void Update()
    {
        if (_triggered && !audio2.isPlaying)
            
        Destroy(this.gameObject);
       
    }
    //Para que las monedas desaparezcan
    void OnTriggerEnter(Collider hit)
    {
        if (hit.gameObject.tag == "Player")
            CoinValue++;
        _triggered = true;
        audio2.enabled = true;
        GameManager.PickUpCount += CoinValue;



    }
    */
}
